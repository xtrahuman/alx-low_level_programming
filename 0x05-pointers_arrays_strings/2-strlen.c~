#include "main.h"
/**
 * _strenlen - returns the length of a string
 * @s: string
 * return: length of string
 */
int _strlen(char *s)
{
	int a;

	a = 0;
	while(s[a] != '\0')
	{
		a++;
	}
	return (a);
}
