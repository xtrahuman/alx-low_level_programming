#include "main.h"
/**
 * print_numbers - prints digit 0-9
 *
 * Return: 1 if upper and 0 if it isnt
 */
void print_numbers(void)
{
	int i;
	for (i = 48; i <= 57; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
